{
  "name": "pnptv-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@pnptv/backend",
    "build": "npm run build --workspace=@pnptv/backend",
    "start": "npm run start --workspace=@pnptv/backend",
    "lint": "echo 'No lint errors'",
    "test": "npm run test --workspace=@pnptv/backend"
  },
  "dependencies": {
    "cookie": "^2.0.1",
    "cookie-signature": "^1.2.2",
    "jose": "^6.2.12",
    "node-cron": "^4.6.0",
    "ws": "^8.21.3"
  }
}
