node_modules/
dist/
.env
.env.production
logs/
backups/
*.log
*.backup-emergency
infrastructure/data/

# User-uploaded media (served from disk, not tracked in git)
public/uploads/
uploads/

# Build & tooling caches
.turbo/
*.tsbuildinfo
.codex

# Private key files — never commit
*.pem

# Runtime-downloaded binaries
apps/backend/yt-dlp_linux
.claude/

# Docs bundle archives (regenerated on demand from apps/web/public/docs/*)
apps/web/public/docs/pnptv-docs.zip
apps/web/dist/docs/pnptv-docs.zip

# Local backups & scratch dirs — not code
*.bak
pitch/
tmp/

# Claude Design sync — staged converter, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
packages/ui-kit/.ds-cache/
backup/

# Operator-local setup notes / device provisioning docs
*.pdf


# Rendered marketing videos (scripts/render-marketing-videos.mjs).
# Large binaries that vite copies into dist on every build - regenerate,
# never commit.
apps/web/public/marketing/*.mp4

# Broadcast campaign videos - large binaries served from public/, not source.
apps/web/public/broadcast/*.mp4
